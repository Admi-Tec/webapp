# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# bun.lock is authoritative (see CLAUDE.md) — don't let a stray `npm install`
# reintroduce a second, drifting lockfile.
package-lock.json

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Vitest coverage reports
coverage

# Local env vars (never commit secrets)
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
